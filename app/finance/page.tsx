import { PlaceholderPage } from "@/components/ui";
import { financePageData } from "@/data/mock/finance";

export default function FinancePage() {
  return <PlaceholderPage {...financePageData} />;
}
